On the docker-compose file set the path of the media volume where your music is located.
Go to ip-address:9790

being ip-address the ip of the computer where the container is running.

-Set the source directory to /Music and click update


Optional:

If you have purchased a licence (or in trial mode):

Install minimstreamer package, then:

Properties>System>stream.converter>/opt/minimserver/bin/ffmpeg
Properties>System>stream.transcode>mp4:L16